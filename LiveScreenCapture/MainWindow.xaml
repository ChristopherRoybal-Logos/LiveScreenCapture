<Window x:Class="LiveScreenCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button x:Name="UpdateColorButton" Content="Toggle Color" Click="UpdateColorButton_Click" />
            <Button x:Name="ToggleTestWindowButton" Content="Toggle Test Window" Click="ToggleTestWindowButton_Click" />
            <Button x:Name="StartCameraButton" Content="Start Camera" Click="StartCameraButton_Click" />
            <Button x:Name="StopCameraButton" Content="Stop Camera" Click="StopCameraButton_Click" />
            <ComboBox x:Name="MonikerComboBox" SelectionChanged="MonikerComboBox_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="FPS: " />
                <TextBox x:Name="FPSTextBox" Text="30" TextChanged="FPSTextBox_TextChanged" MinWidth="50" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Actual FPS: " />
                <TextBlock x:Name="ActualFPSTextBox" Text="NaN" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Column="1" Background="LightSlateGray">
            <Image x:Name="BorderImage" />
        </Border>
    </Grid>
</Window>
